export default class Product {
  constructor(description, byPrice, selPrice, stock) {
    this.description = description;
    this.byPrice = byPrice;
    this.selPrice = selPrice;
    this.stock = stock;
  }
}
